---
import { ADS_ENABLED } from '../config/ads';

interface Props { label?: string; }
const { label = 'Annonsplats' } = Astro.props as Props;
---
{ADS_ENABLED && (
  <div class="ad-slot">
    {label}: <!-- TODO: klistra in <ins class="adsbygoogle">-taggen härifrån AdSense här --> 320×100 / responsiv
  </div>
)}
